{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Project",
            "type": "go",
            "request": "launch",
            "program": "${workspaceFolder}/cmd/main.go",
            "cwd": "${workspaceFolder}",
            "envFile": "${workspaceFolder}/.env",
            "args": [ "--servicename", "http-service-test"],
            "trace": "verbose"
        }
    ]
}
